# ShadowCheck Handoff (2026-03-02)

## Scope

This handoff covers the branch and deployment/debug work around:

- dashboard filter behavior
- smoke harness reliability
- v2 threat-map SQL failures
- branch/rebuild drift on EC2

## Current Branches

- Active fix branch: `chore/deps-safe-align`
- Separate deps branch (WIP): `chore/deps-safe-upgrades`

Do **not** mix these concerns during validation.

## Key Commits On `chore/deps-safe-align`

- `cf1ebf5` test(smoke): add dashboard filter smoke and fix v2 smoke regressions
- `e04ba3f` fix(v2): correct threat-map query parameter binding
- `4f59162` test(smoke): bound threat-map probe and harden endpoint timeouts

## What Was Added/Changed

- Added dashboard-focused smoke script:
  - `scripts/test-dashboard-filters.sh`
- Fixed filter-smoke metadata parsing for current API response shape:
  - `scripts/test-all-filters.sh`
- Updated threat-map SQL in v2 service away from removed `ne.threat` JSON and fixed param binding:
  - `server/src/services/v2Service.ts`
- Hardened endpoint smoke harness for heavy endpoints/timeouts:
  - `scripts/test-endpoints.sh`

## Verified Behavior

- Dashboard filter smoke passes when run correctly:
  - baseline
  - impossible BSSID -> zero totals
  - `radioTypes=["W"]` scopes counts
  - all-radio selection neutralized
- Direct threat-map payload has returned valid structured JSON in manual checks (large payload).

## Outstanding Failures (as last reported)

- `scripts/test-endpoints.sh`
  - `/api/v2/threats/map` intermittent failure/timeout history
  - `/api/ml/score-all` fails with: `missing FROM-clause entry for table "o"`
  - `/api/json` fails with HTTP 500

## Important Operational Notes

- EC2 had repeated branch drift and merge-state interruptions.
- `scs_rebuild` can stop at git/editor prompts; use:
  - `GIT_EDITOR=true scs_rebuild`
- Git identity had to be configured on EC2 to avoid pull/merge failure:
  - `git config user.name "dbcooper"`
  - `git config user.email "dbcooper@users.noreply.github.com"`
- A typo `it fetch` (instead of `git fetch`) caused command-not-found noise.
- One run used invalid URL `http://localhost:3001~` (trailing `~`) causing HTTP 000.

## Recommended Next Steps (for next LLM/operator)

1. Ensure EC2 is aligned to the intended code:
   - `git fetch origin --prune`
   - `git switch -C chore/deps-safe-align origin/chore/deps-safe-align`
   - `git rev-parse --short HEAD` (expect at least `4f59162` or newer)
2. Rebuild from that branch without editor blocking:
   - `GIT_EDITOR=true scs_rebuild`
3. Re-run smoke:
   - `bash scripts/test-endpoints.sh`
   - `bash scripts/test-dashboard-filters.sh http://localhost:3001`
   - Optional: `bash scripts/test-all-filters.sh localhost:3001`
4. If endpoint failures remain, isolate with direct calls:
   - `curl -sS --max-time 30 'http://localhost:3001/api/v2/threats/map?severity=high&days=1' | jq '{meta, threats:(.threats|length), observations:(.observations|length), error}'`
   - `curl -sS -X POST 'http://localhost:3001/api/ml/score-all' -H 'Content-Type: application/json' -d '{"limit":1,"overwrite_final":false}' | jq`
   - `curl -sS 'http://localhost:3001/api/json' | jq '{type:(.|type), ok, error}'`
5. Triage/fix remaining backend regressions:
   - `/api/ml/score-all` SQL alias issue (`o` reference)
   - `/api/json` 500 root cause

## Dependency Upgrade Track

- Keep dependency upgrades isolated in `chore/deps-safe-upgrades`.
- Do not merge deps changes into this fix branch until smoke + backend regressions are stable.
