apiVersion: 1
datasources:
  - name: ShadowCheck-Postgres
    type: postgres
    url: shadowcheck_postgres:5432
    database: shadowcheck_db
    user: shadowcheck_user
    secureJsonData:
      password: "${DB_PASSWORD}"
    jsonData:
      sslmode: disable
      maxOpenConns: 5
      maxIdleConns: 2
      connMaxLifetime: 14400
      postgresVersion: 1500
      timescaledb: false
    isDefault: true
