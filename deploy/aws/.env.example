# ShadowCheck AWS Deployment Configuration
# Copy this file to .env.aws and fill in your values

# Database Configuration
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USER=shadowcheck_user
DB_PASSWORD=<get_from_postgres_container>
DB_NAME=shadowcheck_db
DB_SSL=true

# Application Configuration
NODE_ENV=development
PORT=3001

# API Keys
MAPBOX_TOKEN=<your_mapbox_token_from_https://account.mapbox.com/access-tokens/>

# CORS Configuration (comma-separated list)
# Replace with your EC2 public IP
CORS_ORIGINS=http://13.216.239.240,http://localhost

# AWS Configuration
AWS_REGION=us-east-1
INSTANCE_ID=i-035565c52ac4fa6dd
SECURITY_GROUP_ID=sg-076801a316243fa70

# Notes:
# - Use NODE_ENV=development for HTTP (testing)
# - Use NODE_ENV=production only with HTTPS setup
# - Get DB_PASSWORD: docker exec shadowcheck_postgres printenv POSTGRES_PASSWORD
# - Get public IP: curl http://169.254.169.254/latest/meta-data/public-ipv4
