These standalone query files are archived reference assets.

They are not called by the canonical server, ETL, or migration paths and are
kept only for manual analysis/history. Active product SQL should live in the
server, ETL, or migration codepaths instead of this directory.
