# ShadowCheck Environment Configuration
# Copy this file to .env and fill in your values

# Database Configuration
DB_USER=shadowcheck_user
DB_ADMIN_USER=shadowcheck_admin
DB_HOST=shadowcheck_postgres
DB_NAME=shadowcheck_db
DB_PORT=5432

# Server Configuration
PORT=3001
NODE_ENV=development

# HTTPS (optional, for production)
FORCE_HTTPS=false

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:3001,http://127.0.0.1:3001

# Threat Detection
THREAT_THRESHOLD=40
MIN_OBSERVATIONS=2

# Admin Docker Controls (optional)
# ADMIN_ALLOW_DOCKER=true
# PGADMIN_COMPOSE_FILE=/absolute/path/to/docker/infrastructure/docker-compose.postgres.yml

# ============================================================================
# SECRETS MANAGEMENT
# ============================================================================
# Secrets are loaded in this order:
# 1. Docker secrets (/run/secrets/*)
# 2. System keyring (via keyringService)
# 3. Environment variables (below)
#
# For production, use Docker secrets or keyring.
# For development, you can use environment variables below.
# ============================================================================

# Required Secrets (must be set via one of the methods above)
# DB_PASSWORD=your_secure_password_here
# DB_ADMIN_PASSWORD=your_admin_password_here
# MAPBOX_TOKEN=pk.your_mapbox_token_here

# Optional Secrets (for enhanced features)
# API_KEY=your_secret_api_key_here
# WIGLE_API_KEY=your_wigle_api_key
# WIGLE_API_TOKEN=your_wigle_api_token
# LOCATIONIQ_API_KEY=your_locationiq_key
# OPENCAGE_API_KEY=your_opencage_key
# SMARTY_AUTH_ID=your_smarty_auth_id
# SMARTY_AUTH_TOKEN=your_smarty_auth_token

# To use keyring (recommended for local development):
# node scripts/keyring-cli.js set db_password
# node scripts/keyring-cli.js set mapbox_token
# node scripts/keyring-cli.js set api_key

# ============================================================================
# VITE FRONTEND ENVIRONMENT VARIABLES
# ============================================================================
# These variables are used by the Vite frontend and must be prefixed with VITE_
# Copy these to .env.local for local development
#
# VITE_MAPBOX_TOKEN=your_mapbox_token_here
# VITE_API_BASE=http://localhost:3001
# VITE_DEV_MODE=true
